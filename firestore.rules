rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access for portfolio content synchronization
    match /portfolio_content/{docId} {
      allow read, write: if true;
    }
  }
}
